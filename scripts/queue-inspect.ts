/**
 * Queue inspector — prints pgmq queue depths.
 * Wired up properly once Phase 3's queue topology exists.
 */

function main(): void {
  console.warn('queue:inspect is filled in during Phase 3; not implemented yet.');
  process.exit(0);
}

main();
